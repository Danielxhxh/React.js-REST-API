An extremely simple example of a React app consuming a REST API using the axios package.
Meanwhile, the REST API , when the client makes GET request to the "/" route, it uses the Https package included in 
node.js to make requests to an external server.
The project is suddivided in 2 folders:
1) Frontend - containes React
2) Backend - containes our RESTful API